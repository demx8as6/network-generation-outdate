network-generation
==================

Small node application to generate a telecommunication network in json format.
The network should be displayed on a map. Therefore the network sites geographical coordinations of the generated site objects must fit to the geographical coordinations of a country. 

The resulting json file will be stored in 'target/network.json'